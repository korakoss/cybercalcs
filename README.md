# Calculations for the GovAI cybercrime estimate

The data file `full_survey.csv` is from the code release by Breen et al. The notebook `breen_analysis.ipynb` contains the main calculations for the the consumer loss estimate errors, while `business_lognormals.squiggle` is a small model of my own referenced in the discussion of the business loss estimate.